[project]
name = "musique-backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.2",
    "mutagen>=1.48.1",
    "uvicorn[standard]>=0.51.0",
]
